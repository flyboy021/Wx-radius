import React from "react";

export interface LegendProps {
  style?: React.CSSProperties;
}

/**
 * Four-dot flight-category key (VFR / MVFR / IFR / LIFR).
 */
export function Legend(props: LegendProps): JSX.Element;
